

## Footer Background Color Update

Change the Footer component's background from the current gradient to a solid `rgb(134, 25, 28)` (#86191C).

### Changes

**File: `src/components/Footer.tsx`**
- Replace the current background gradient/color on the footer container with `bg-[#86191C]` (or inline style `backgroundColor: rgb(134,25,28)`).

Single-file, single-line change.

